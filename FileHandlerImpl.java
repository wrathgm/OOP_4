class FileHandlerImpl implements FileHandler {
    @Override
    public void saveToFile(Tree<?> tree, String fileName) {
        // Логика сохранения дерева в файл
    }

    @Override
    public Tree<Person> loadFromFile(String fileName) {
        // Логика загрузки дерева из файла
        return null;
    }
}
